{
  "name": "nonupo.io",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "yarn --cwd server start",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  }
}
